/**
 * Make sure to import the desired prototype as the App
 */

import { AppRegistry } from 'react-native';
import App from './prototypes/realm';

AppRegistry.registerComponent('RNPrototypes', () => App);
